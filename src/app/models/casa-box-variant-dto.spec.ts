import { CasaBoxVariantDto } from './casa-box-variant-dto';

describe('CasaBoxVariantDto', () => {
  it('should create an instance', () => {
    expect(new CasaBoxVariantDto()).toBeTruthy();
  });
});
